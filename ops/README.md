# Ops

This directory is reserved for deployment and infrastructure assets.

Add future team-owned operational material here, such as:
- container definitions
- infrastructure-as-code
- deployment manifests
- environment templates
- release automation

Keep application source code in `web/` or `backend/`; keep shared documentation in `docs/`.
