spring:
  datasource:
    url: jdbc:h2:mem:ictcrakr-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

ictcrakr:
  frontend-origin: http://localhost:3000
